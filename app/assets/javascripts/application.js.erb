// This is a manifest file that'll be compiled into application.js, which will include all the files
// listed below.
//
// Any JavaScript/Coffee file within this directory, lib/assets/javascripts, vendor/assets/javascripts,
// or vendor/assets/javascripts of plugins, if any, can be referenced here using a relative path.
//
// It's not advisable to add code directly here, but if you do, it'll appear at the bottom of the
// compiled file.
//
// Read Sprockets README (https://github.com/sstephenson/sprockets#sprockets-directives) for details
// about supported directives.

//= require jquery
//= require jquery_ujs
//= require bootstrap
//= require bootstrapselect
//= require turbolinks
//= require_tree .

$(document).ready(function() {

	$('#notes').on("click", "li", function() {
    $.ajax({url: "/notes/" + $(this).data("note-id") + "/edit", dataType: "script"});
    
  });
  $('.selectpicker').selectpicker();
  $('.glyphicon-heart').click(function() {
    $(this)
        .css('color','#E36A5B');
    });

});


 
//    $('.listlineitem').click(function() {
//     var name = $(this).find('span p.list-name').text();
//     var category = $(this).find('span p.list-category').text();
//     var calories = parseInt($(this).find('span p.list-calories').text().replace('Calories: ', ''));
//     var fat = $(this).find('span p.list-fat').text();
//     $('#foodpagefooter').append("<div class = 'container'><span class='footer-list-name'>"+name+"</span><span class = 'footer-list-calories'>"+calories+"</span></div>");
    
//     var sum = 0.0;
//     $('.footer-list-calories').each(function()
//       {
//      sum += parseFloat($(this).text());
//         });
//       $('#foodpagefooter').append("<div class= 'sum'>"+sum+"></div>");
//   });

   
// });
