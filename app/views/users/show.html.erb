<div class="profile">
	<div class="container">
		<h1 class="profile-header">Hi <%= @user.name.capitalize %></h1>
		<p><%= gravatar_for @user %></p>
		<p><%= @user.email %></p>
		<p><%= link_to "Edit profile", edit_user_url(current_user) %></p>
		<p>You haven't added any favorites yet. <%= link_to "Add some now", root_url %></p>
	</div>
</div>
